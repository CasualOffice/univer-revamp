{
  "labels": [
    {
      "name": "area:slides",
      "color": "1d76db",
      "description": "Univer slides / presentation editor"
    },
    {
      "name": "fidelity:visual",
      "color": "5319e7",
      "description": "What the user sees: shapes, fills, effects, masters"
    },
    {
      "name": "fidelity:editable",
      "color": "0e8a16",
      "description": "What the user can change: editing UX"
    },
    {
      "name": "fidelity:tags",
      "color": "fbca04",
      "description": "Semantic structure: placeholders, a11y, links, metadata"
    },
    {
      "name": "fidelity:roundtrip",
      "color": "b60205",
      "description": "Lossless import/export: PPTX, ODP, PDF"
    },
    {
      "name": "fidelity:collab",
      "color": "006b75",
      "description": "Real-time multi-user (Yjs + Hocuspocus)"
    },
    {
      "name": "phase:0",
      "color": "c5def5",
      "description": "Foundation"
    },
    {
      "name": "phase:1",
      "color": "c5def5",
      "description": "Editing core + visual base"
    },
    {
      "name": "phase:2",
      "color": "c5def5",
      "description": "Round-trip"
    },
    {
      "name": "phase:3",
      "color": "c5def5",
      "description": "Collaboration + self-host"
    },
    {
      "name": "phase:4",
      "color": "c5def5",
      "description": "Advanced fidelity"
    }
  ],
  "milestones": [
    {
      "title": "Phase 0 — Foundation",
      "description": "Make every edit a real, collab-ready, undoable mutation."
    },
    {
      "title": "Phase 1 — Editing core + visual base",
      "description": "Feels like a real editor: multi-select, copy/paste, group, align, rich text, shape styling, full shape geometry."
    },
    {
      "title": "Phase 2 — Round-trip",
      "description": "Lossless PPTX import/export, ODP, PDF export, and the data-model extensions they require."
    },
    {
      "title": "Phase 3 — Collaboration + self-host",
      "description": "Yjs CRDT over a self-hosted Hocuspocus server: presence, comments, persistence."
    },
    {
      "title": "Phase 4 — Advanced fidelity",
      "description": "Animations/transitions, charts, SmartArt, media, accessibility."
    }
  ],
  "issues": [
    {
      "slug": "p0-mutation-coverage-audit",
      "title": "[slides][p0] Close edit paths that bypass the mutation layer",
      "milestone": "Phase 0 — Foundation",
      "labels": [
        "area:slides",
        "fidelity:editable",
        "fidelity:collab",
        "phase:0"
      ],
      "body": "## Pillar\nEditable / Collab foundation.\n\n## Problem\nThe `slide/element-mutations` branch routed insert/update/delete element and page edits through `CommandType.MUTATION`, which is the precondition for undo/redo and (later) Yjs sync. But some edit paths still execute as plain `CommandType.OPERATION` and never touch the persisted snapshot:\n- `commands/operations/text-edit.operation.ts` — `SetTextEditArrowOperation` is a no-op stub (`handler: () => true`).\n- `commands/operations/set-thumb.operation.ts` — thumbnail writes.\n- In-place text editing in `controllers/slide-editing.render-controller.ts` — verify the doc text changes land as slide mutations, not transient editor state.\n\n## Scope\n- Inventory every command/operation under `packages/slides-ui/src/commands` and classify: mutates persisted `ISlideData`? → must be (or be wrapped by) a `MUTATION`.\n- Convert or wrap the offenders; ensure each has an inverse mutation for undo/redo.\n- Add a unit test asserting that executing each user-facing edit produces a mutation observable via `ICommandService` mutation hooks.\n\n## Acceptance criteria\n- No persisted-state edit runs purely as `OPERATION`.\n- Undo/redo covers text edits and thumbnail changes.\n- Test proves mutations are emitted for every edit type.\n\n## Key files\n`packages/slides-ui/src/commands/**`, `packages/slides-ui/src/controllers/slide-editing.render-controller.ts`, `packages/slides/src/data-model/slide-data-model.ts`"
    },
    {
      "slug": "p0-snapshot-contract",
      "title": "[slides][p0] Canonical ISlideData (de)serialization contract + schema version",
      "milestone": "Phase 0 — Foundation",
      "labels": [
        "area:slides",
        "fidelity:roundtrip",
        "phase:0"
      ],
      "body": "## Pillar\nRound-trip foundation.\n\n## Problem\nRound-trip (PPTX/ODP) and collab both need one canonical, versioned in-memory model. Today `ISlideData` has `@deprecated` fields (`spreadsheet`, `document`, `slide`) and commented-out future types (`video`, `line`, `table`, `chart`). There's no schema version and no documented (de)serialization invariant.\n\n## Scope\n- Add a `schemaVersion` to `ISlideData` and a migration shim.\n- Define and document the canonical JSON snapshot: round-tripping `toSnapshot()` → `fromSnapshot()` must be lossless and stable (key order, ids).\n- Remove/relocate deprecated element shapes; reserve namespaces for `table`/`chart`/`media`/`line` so later phases extend without breaking.\n- Property-based test: random valid `ISlideData` survives snapshot round-trip byte-stable.\n\n## Acceptance criteria\n- `schemaVersion` present; migration covered by tests.\n- Documented invariant + passing round-trip test.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, `packages/slides/src/data-model/slide-data-model.ts`"
    },
    {
      "slug": "p1-multi-select",
      "title": "[slides][p1] Multi-select + marquee selection of page elements",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:editable",
        "phase:1"
      ],
      "body": "## Pillar\nEditable.\n\n## Problem\nOnly a single element can be selected. Every higher-order editing feature (group, align, distribute, batch style, multi-delete, multi-move) depends on multi-select, which does not exist.\n\n## Scope\n- Selection model holding N elements; shift/ctrl-click additive selection.\n- Marquee (drag-rectangle) selection on the page scene.\n- Combined bounding box + group transform handles for the selection.\n- Wire delete/move/resize to operate over the whole selection via batched mutations.\n\n## Acceptance criteria\n- Can select multiple elements, see a unified transform box, move/resize/delete them as one undoable action.\n\n## Key files\n`packages/slides-ui/src/controllers/**`, `packages/engine-render/src/scene.input-manager.ts`, selection services."
    },
    {
      "slug": "p1-group-ungroup",
      "title": "[slides][p1] Group / ungroup elements",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "fidelity:editable",
        "phase:1"
      ],
      "body": "## Pillar\nEditable + Visual (groups are a real PPTX element type, `p:grpSp`).\n\n## Problem\nNo grouping. PPTX uses group shapes extensively; without a group element type, import/export will be lossy and editing nested structures is impossible.\n\n## Scope\n- Add a `group` page-element type to `ISlideData` with child elements + group transform.\n- Group/ungroup commands (mutation-backed).\n- Render adaptor for groups (nested transform composition) in `views/render/adaptors`.\n- Selecting a group selects/moves children together; double-click to enter group.\n\n## Acceptance criteria\n- Group/ungroup works, renders with correct nested transforms, and round-trips through the snapshot.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, `packages/slides/src/views/render/adaptors/**`, `packages/slides-ui/src/commands/**`."
    },
    {
      "slug": "p1-shape-geometry",
      "title": "[slides][p1] Render the full preset-shape geometry set (50+ shapes)",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "phase:1"
      ],
      "body": "## Pillar\nVisual.\n\n## Problem\nOnly `Rect`, `RoundRect`, `Ellipse` render (~6% of the geometries already enumerated in `prst-geom-type.ts`). PowerPoint's `prstGeom` preset library (lines, triangles, arrows, stars, callouts, banners, etc.) is unrendered, so most real decks degrade to nothing.\n\n## Scope\n- Add a preset-geometry → path system in `engine-render` (SVG-path / parametric guides per OOXML preset).\n- Extend `ShapeAdaptor` to dispatch all `BasicShapes`/`ArrowsAndMarkersShapes`/`OtherShapes`/`SpecialShapes` to the path renderer.\n- Support adjustable handles (the OOXML `avLst` adjust values) where feasible.\n- Shape-picker UI exposing the catalog.\n\n## Acceptance criteria\n- All enumerated preset shapes render with correct geometry, fill, and outline; insertable from the picker.\n\n## Key files\n`packages/slides/src/views/render/adaptors/shape-adaptor.ts`, `packages/slides/src/types/enum/prst-geom-type.ts`, `packages/engine-render/src/shape/**`."
    },
    {
      "slug": "p1-gradient-shadow-fills",
      "title": "[slides][p1] Gradient / pattern / picture fills + shadow & effect rendering",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "phase:1"
      ],
      "body": "## Pillar\nVisual.\n\n## Problem\n`IColorStyle` is solid-RGB-or-theme only. No gradient, pattern, or picture fills. Shape shadows exist in `engine-render` but the slide adaptor never passes them. No reflection/glow/soft-edge.\n\n## Scope\n- Extend the fill model to a discriminated union: solid | gradient (linear/radial w/ stops) | pattern | picture.\n- Map gradient/pattern/picture fills to `engine-render` paint.\n- Pass shadow props (`shadowColor/Blur/Offset/Opacity`) from `IShapeProperties` through the adaptor.\n- Convert `IOutline.dashStyle` → `strokeDashArray`.\n\n## Acceptance criteria\n- Gradient/picture fills and shadows render; dash styles render; covered by visual/snapshot tests.\n\n## Key files\n`packages/core/src/types/interfaces/i-color-style.ts`, `packages/slides/src/views/render/adaptors/shape-adaptor.ts`, `packages/engine-render/src/shape/**`."
    },
    {
      "slug": "p1-image-adjust-crop",
      "title": "[slides][p1] Wire image brightness/contrast/transparency/crop to the renderer",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "phase:1"
      ],
      "body": "## Pillar\nVisual.\n\n## Problem\n`IImageProperties` models `brightness`, `contrast`, `transparency`, and `cropProperties`, but `ImageAdaptor` never passes them to the `Image` engine object — they're silently dropped, so imported images lose adjustments/crop.\n\n## Scope\n- Apply brightness/contrast/transparency as canvas filters in the image render path.\n- Implement crop (offsetLeft/Right/Top/Bottom + angle) in the adaptor.\n- Add minimal crop UI in the image popup menu.\n\n## Acceptance criteria\n- Adjustments and crop render correctly and survive snapshot round-trip.\n\n## Key files\n`packages/slides/src/views/render/adaptors/image-adaptor.ts`, `packages/core/src/types/interfaces/i-image-properties.ts`, `packages/slides-ui/src/views/image-popup-menu/**`."
    },
    {
      "slug": "p2-masters-layouts-theme",
      "title": "[slides][p2] Slide masters, layouts, placeholders & theme color resolution",
      "milestone": "Phase 2 — Round-trip",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "fidelity:tags",
        "phase:2"
      ],
      "body": "## Pillar\nVisual + Tags.\n\n## Problem\n`IReferenceSource` already carries `master`, `layouts`, `notesMaster`, `handoutMaster` and elements carry a `placeholder` field — but none of it is rendered or resolved. Theme colors (`IColorStyle.th`) aren't resolved either. Without this, PPTX import is hollow (most deck content lives on layouts/masters).\n\n## Scope\n- Render pipeline resolves placeholder inheritance: element → layout placeholder → master placeholder.\n- Resolve theme color references to concrete RGB via the presentation theme.\n- Master/layout editing surface (at least selection of a layout per slide).\n\n## Acceptance criteria\n- Slides inherit text/shape defaults from layout+master; theme colors resolve; switching layout updates the slide.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, `packages/slides/src/views/render/**`."
    },
    {
      "slug": "p2-tables",
      "title": "[slides][p2] Table element: model, render & edit",
      "milestone": "Phase 2 — Round-trip",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "fidelity:editable",
        "phase:2"
      ],
      "body": "## Pillar\nVisual + Editable (required for PPTX round-trip).\n\n## Problem\nTables are commented out in `i-slide-data.ts`. PPTX `a:tbl` is common; without tables, import is lossy.\n\n## Scope\n- Add a `table` page-element type (rows/cols, cell spans, per-cell fill/border/text, column widths/row heights).\n- Render adaptor for tables.\n- Basic editing: add/remove row/col, edit cell text, cell styling.\n- Map to/from OOXML `a:tbl`.\n\n## Acceptance criteria\n- Tables render, are minimally editable, and round-trip through PPTX.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, `packages/slides/src/views/render/adaptors/**`."
    },
    {
      "slug": "p1-element-clipboard",
      "title": "[slides][p1] Element clipboard: copy/cut/paste/duplicate as MUTATION commands",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:editable",
        "phase:1"
      ],
      "body": "## Pillar\nEditable (engine core). **Reframed** from the original copy/paste issue to the engine-owned part.\n\n## Problem\nThere is no element-level clipboard. `duplicate-slide` already landed as a mutation on this branch, but there is no copy/cut/paste/duplicate for *elements*. The engine must provide these as proper COMMAND+MUTATION pairs so they are undoable and collab-safe; the product shell binds the shortcuts/menu.\n\n## Scope (engine)\n- `slide.command.copy/cut/paste/duplicate-element` → `SlideInsertElementMutation` / `SlideDeleteElementMutation` with fresh ids + paste offset; cross-slide aware.\n- In-engine element clipboard buffer; expose via facade so the product can trigger it.\n- Multi-element aware (depends on #3 multi-select).\n\n## Out of scope (product `point`)\n- OS-clipboard integration (paste image/text from the OS), keyboard-shortcut binding, and menu UI live in the product shell.\n\n## Acceptance criteria\n- Copy/cut/paste/duplicate of one or many elements works within and across slides, all undoable, all emitting mutations.\n\n## Key files\n`packages/slides-ui/src/commands/**`, `packages/slides-ui/src/commands/mutations/element.mutation.ts`."
    },
    {
      "slug": "p1-smart-guides-snapping",
      "title": "[slides][p1] Canvas smart-guides + snapping during drag/resize (engine-render)",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:editable",
        "phase:1"
      ],
      "body": "## Pillar\nEditable + Canvas rendering (engine core). **Reframed** from the original align/distribute issue.\n\n## Problem\nThere are no alignment guides or snapping while dragging/resizing elements — precise layout is guesswork. The *guides + snapping* are a canvas-rendering concern that must live in `engine-render`/`slides-ui`; the align/distribute *buttons* are product UI that just set transforms via the facade.\n\n## Scope (engine)\n- Smart guides: detect + draw alignment lines to other elements' edges/centers and to slide center/edges during drag/resize.\n- Snapping: snap transform to those guides within a threshold; modifier key to bypass.\n- Optional grid + snap-to-grid.\n\n## Out of scope (product `point`)\n- Align/distribute toolbar buttons (call `FElement` transforms via the facade).\n\n## Acceptance criteria\n- Dragging an element shows guides and snaps to neighbors/slide; mutation-backed final position.\n\n## Key files\n`packages/engine-render/src/scene.input-manager.ts`, slides-ui render controllers."
    },
    {
      "slug": "p2-hyperlinks-semantic-fields",
      "title": "[slides][p2] Hyperlink rendering + element semantic fields (alt-text / description)",
      "milestone": "Phase 2 — Round-trip",
      "labels": [
        "area:slides",
        "fidelity:tags",
        "phase:2"
      ],
      "body": "## Pillar\nTags (engine core). **Reframed** — the product owns speaker-notes UI and presentation metadata; the engine owns the *renderable/structural* semantics.\n\n## Problem\n`ILink` exists for navigation but isn't rendered/clickable, and the per-element `description`/`title` fields (alt text) are dead metadata — never populated or surfaced. Both must round-trip and both are needed for accessibility.\n\n## Scope (engine)\n- Render `ILink` on elements + text runs as clickable (internal slide nav + external URLs).\n- Treat element `description`/`title` as first-class alt-text in the model; expose via facade so the product can edit and so PPTX import/export can map them.\n\n## Out of scope (product `point`)\n- Speaker-notes editing pane (Gap 5), presentation core/app metadata (Gap 7 I/O), a11y checker UI (Gap 26-equivalent).\n\n## Acceptance criteria\n- Links are clickable in the canvas; alt-text fields are read/writable via facade and survive snapshot round-trip.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, render adaptors, facade."
    },
    {
      "slug": "p4-element-types-chart-line-video",
      "title": "[slides][p4] Element-type data model + render: chart, line/connector, video (Gap 3)",
      "milestone": "Phase 4 — Advanced fidelity",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "phase:4"
      ],
      "body": "## Pillar\nVisual (engine core). **Reframed** to the engine data-model + render part of product Gap 3. (Group = #5, table = #17 cover the rest of Gap 3.)\n\n## Problem\n`PageElementType` is missing `LINE`, `CHART`, `VIDEO` — they're commented out in `i-slide-data.ts`. Without these element types in the engine, PPTX round-trip drops connectors/charts/media (blocks T3 fidelity), and the product can't render or preserve them.\n\n## Scope (engine)\n- Add `LINE` (connector endpoints + routing), `CHART` (at minimum a preserve-and-render element backed by a cached drawing; ideally leverage Univer sheets/chart infra), and `VIDEO` (poster + media ref) to `PageElementType`.\n- Render adaptors for each in `views/render/adaptors`.\n- Suggested order from product plan: lines (~1wk) → charts (~2wk) → video (~1wk).\n\n## Out of scope (product `point`)\n- SmartArt / chart-data editing UX and raw-OOXML passthrough live in the product I/O layer.\n\n## Acceptance criteria\n- The three element types are modeled, render on canvas, and survive snapshot round-trip.\n\n## Key files\n`packages/slides/src/types/interfaces/i-slide-data.ts`, `packages/slides/src/types/enum/**`, `packages/slides/src/views/render/adaptors/**`."
    },
    {
      "slug": "p1-facade-api",
      "title": "[slides][p1] Slides facade API: FSlide / FPage / FElement (Gap 8)",
      "milestone": "Phase 1 — Editing core + visual base",
      "labels": [
        "area:slides",
        "fidelity:editable",
        "phase:1"
      ],
      "body": "## Pillar\nEditable (engine core). Product Gap 8.\n\n## Problem\nThe slides facade is partial compared to sheets' `FWorkbook`/`FWorksheet`. The product shell (`point`) needs a stable public API to drive edits instead of reaching into internal commands/mutations — otherwise every UI feature couples to engine internals and every fork bump risks breakage.\n\n## Scope (engine)\n- Mirror the sheet facade pattern: `FSlide` (presentation), `FPage` (slide/layout/master page), `FElement` (shape/text/image/group/table/...).\n- Cover: create/read/update/delete elements + pages, transforms, z-order, styling props, text content, element clipboard (#4), alt-text/links (#18), selection.\n- Incremental, upstream-eligible PRs (per product Gap 8, ~1–2 weeks).\n\n## Acceptance criteria\n- The product can perform all P1 editing operations through the facade with no direct command-id usage.\n\n## Key files\nnew `packages/slides/src/facade/**` (mirror `packages/sheets/src/facade/**`)."
    },
    {
      "slug": "p0-render-context-cleanup",
      "title": "[slides][p0] Fix stale doc-renderer activation after disposeUnit (Gap 1.6)",
      "milestone": "Phase 0 — Foundation",
      "labels": [
        "area:slides",
        "fidelity:visual",
        "phase:0"
      ],
      "body": "## Pillar\nVisual / stability (engine core). Product Gap 1.6.\n\n## Problem\n`packages/slides-ui/src/controllers/slide-editing.render-controller.ts` (~line 257) calls `.activate(...)` on a doc renderer that may already be disposed after `disposeUnit`, producing console noise and a black-screen risk on unit swap. Related to why the product had to abandon hot-swap and remount the whole component (Gap 1.8).\n\n## Scope (engine)\n- Guard activation against a disposed/invalid render context; use `renderContext.unit` rather than stale lookups.\n- Verify clean teardown on `disposeUnit` so the product can hot-swap snapshots without a full remount.\n\n## Acceptance criteria\n- Swapping the slide unit produces no stale-renderer errors; no black screen; hot-swap path viable.\n\n## Key files\n`packages/slides-ui/src/controllers/slide-editing.render-controller.ts`."
    }
  ]
}
